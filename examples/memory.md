# Injected Working Context

This synthetic compact context stands in for injected `MEMORY.md` and `USER.md` surfaces plus profile-local current context.
It is working memory for the active run, not a durable fact-store record.
