# Restricted Strategy Note

This synthetic restricted note represents content that a public demo role should not see.
If retrieval finds it for an unauthorized role, the packet must include only the blocked source id and reason.
